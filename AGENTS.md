# Agents Repo

- Install deps: `npm install`
- Format: `npm run format`
- Lint: `npm run lint`
- Typecheck: `npm run typecheck`
- Verify all checks: `npm run check`
- For local Pi testing, run `pi` from this repo root; `.pi/settings.json` loads the package from the repo itself.
