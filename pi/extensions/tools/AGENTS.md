# Tools Guidance

Do not set `promptSnippet` on custom tools. Put tool purpose and invocation semantics in `description`; reserve `promptGuidelines` for when-to-use policy, sequencing, and other behavior not captured by the tool schema.
