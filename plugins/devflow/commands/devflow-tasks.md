---
description: Jump to the devflow tasks phase
argument-hint: describe the feature/change to slice into AFK tasks
disable-model-invocation: true
---

Load `plugins/devflow/skills/devflow/SKILL.md`.

Jump to the tasks phase. Use the Devflow lifecycle skill to orient, choose or create the feature folder, ensure `proposal.md` exists, and ensure a reviewed `<feat-name>.plan.md` exists. For small obvious queued work, create a minimal plan and mark it Reviewed after a lightweight sanity check. Then load `plugins/devflow/skills/task-authoring/SKILL.md` and create or update `devflow/<feat-name>/tasks/index.yml` plus task files.

User request:

$ARGUMENTS
