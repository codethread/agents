---
description: Jump to the devflow tasks stage
argument-hint: describe the feature/change to slice into AFK tasks
disable-model-invocation: true
---

Load `plugins/devflow/skills/devflow/SKILL.md`.

Jump to the Tasks stage in the Devflow lifecycle. Use the Devflow skill to orient, choose or create the feature folder, ensure `proposal.md` exists, and ensure a reviewed `<feat-name>.plan.md` exists. For small obvious queued work, create a minimal plan and mark it Reviewed after a lightweight sanity check. Then follow the tasks reference named in the skill's reference table to create or update `devflow/feat/<feat-name>/tasks/index.yml` plus task files.

User request:

$ARGUMENTS
