---
description: Jump to the devflow spec phase
argument-hint: describe the root spec, feature-local spec, or delta to create/update
disable-model-invocation: true
---

Load `plugins/devflow/skills/devflow/SKILL.md`.

Jump to the spec phase. Use the Devflow lifecycle skill to orient and decide whether the user needs a canonical root spec in `devflow/specs/`, a feature-local new spec in `devflow/<feat-name>/specs/`, or a feature-local delta in `devflow/<feat-name>/specs/*.delta.md`. Then load `plugins/devflow/skills/spec-authoring/SKILL.md` and create, update, or promote the spec artifact.

User request:

$ARGUMENTS
