---
description: Jump to the devflow spec stage
argument-hint: describe the root spec, feature-local spec, or delta to create/update
disable-model-invocation: true
---

Load `plugins/devflow/skills/devflow/SKILL.md`.

Jump to the Spec work stage in the Devflow lifecycle. Use the Devflow skill to orient and decide whether the user needs a canonical root spec in `devflow/specs/`, a feature-local new spec in `devflow/feat/<feat-name>/specs/`, or a feature-local delta in `devflow/feat/<feat-name>/specs/*.delta.md`. Then follow the spec reference named in the skill's reference table to create, update, or promote the spec artifact.

User request:

$ARGUMENTS
